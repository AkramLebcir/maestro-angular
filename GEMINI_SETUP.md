# إعداد مولد المذكرات الذكي بالذكاء الاصطناعي (Gemini API)

## نظرة عامة

تم إضافة ميزة **مولد المذكرات الذكي بالذكاء الاصطناعي** التي تستخدم Google Gemini API لتوليد خطط الدروس (المذكرات) تلقائياً وفق النموذج المطلوب.

## الحصول على مفتاح Gemini API

### الخطوات:

1. **زيارة Google AI Studio:**
   - اذهب إلى: https://makersuite.google.com/app/apikey
   - أو: https://aistudio.google.com/app/apikey

2. **تسجيل الدخول:**
   - استخدم حساب Google الخاص بك

3. **إنشاء مفتاح API جديد:**
   - اضغط على "Create API Key"
   - اختر المشروع (أو أنشئ مشروعاً جديداً)
   - سيتم إنشاء مفتاح API تلقائياً

4. **نسخ المفتاح:**
   - انسخ المفتاح الذي يظهر (يبدأ عادة بـ `AIza...`)
   - **احفظه بشكل آمن - لن تتمكن من رؤيته مرة أخرى!**

## إعداد Backend

### إضافة المفتاح في ملف `.env`:

في مجلد `nest-backend/`، أضف أو حدث ملف `.env`:

```env
# Gemini AI Configuration
GEMINI_API_KEY=your_gemini_api_key_here
```

**مثال كامل لملف `.env`:**
```env
# Database Configuration
DB_HOST=localhost
DB_PORT=15432
DB_USERNAME=postgres
DB_PASSWORD=postgres
DB_NAME=nest_db

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key
JWT_EXPIRES_IN=4h

# Gemini AI Configuration
GEMINI_API_KEY=AIzaSyA1B2C3D4E5F6G7H8I9J0K1L2M3N4O5P6Q

# Node Environment
NODE_ENV=development
```

**ملاحظة:** لا تشارك مفتاح API أبداً أو ترفعه إلى Git! تأكد من إضافته إلى `.gitignore`.

## إعادة تشغيل التطبيق

### Backend:
```bash
cd nest-backend
npm run start:dev
```

### Frontend:
```bash
cd angular-frontend
npm start
```

## استخدام الميزة

1. **افتح صفحة الوثائق البيداغوجية:**
   - من القائمة الجانبية، اختر "الوثائق البيداغوجية"

2. **انقر على زر "✨ إنشاء مذكرة جديدة":**
   - سيفتح نموذج إدخال

3. **املأ الحقول المطلوبة:**
   - **اختيار القسم (اختياري):** يمكنك اختيار قسم من قائمة الأقسام لتعبئة المستوى والشعبة تلقائياً
   - **المستوى:** مثل "3 ثانوي"
   - **الشعبة:** مثل "علوم تجريبية"
   - **المجال المفاهيمي (الميدان):** مثل "المجال الأول"
   - **الوحدة المفاهيمية (المقطع):** مثل "الوحدة الثانية"
   - **عنوان الدرس:** العنوان الكامل للدرس
   - **الكفاءة المستهدفة (الهدف):** ما يجب أن يتعلمه التلميذ
   - **مستوى القسم:** اختر من (ضعيف، متوسط، ممتاز) - سيتم تكييف الاستراتيجيات والأنشطة حسب المستوى

4. **انقر على "✨ توليد المذكرة":**
   - سيتم إرسال الطلب إلى Gemini API
   - انتظر حتى يتم توليد المذكرة (عادة 5-15 ثانية)

5. **مراجعة وتعديل المذكرة:**
   - سيتم عرض المذكرة في جدول
   - يمكنك تعديل جميع الحقول يدوياً
   - يمكنك إعادة توليد المذكرة إذا لم تعجبك النتائج

6. **تصدير PDF:**
   - انقر على "📄 تصدير PDF"
   - سيتم حفظ المذكرة كملف PDF بنفس تنسيق نموذج خطة الدرس

## الميزات

### ✨ تكييف المحتوى حسب مستوى القسم:
- **ضعيف:** يركز على الأنشطة التبسيطية، أمثلة واقعية، استراتيجيات بصرية
- **متوسط:** أنشطة متوازنة، استراتيجيات تفاعلية، شرح واضح
- **ممتاز:** وضعيات مشكلة معقدة، استراتيجيات متقدمة، تفكير نقدي وإبداعي

### 📋 المراحل الأربع:
1. **التزام:** وضعية مشكلة (بناء المعرفة المسبقة، والإثارة في الدرس)
2. **تمثيل:** طريقة إلقاء الدرس بالتفصيل مع الاستراتيجيات
3. **مشاركة:** تفاعل التلاميذ مع الدرس
4. **التقييم:** تقييم ما إذا كان الهدف قد تحقق

### 🔗 الربط مع إدارة الأقسام:
- يمكن اختيار قسم من القائمة لتعبئة المستوى والشعبة تلقائياً

### 📝 الاستراتيجيات المقترحة:
- العمل بالأفواج
- التعلم بالقرين
- العصف الذهني
- التعلم بالمشاريع
- التعلم التعاوني
- التعلم النشط
- استخدام الوسائط المتعددة

## استكشاف الأخطاء

### خطأ: "Gemini API key is not configured"
- **الحل:** تأكد من إضافة `GEMINI_API_KEY` في ملف `.env` في مجلد `nest-backend`

### خطأ: "فشل في توليد خطة الدرس"
- **الحل:** 
  - تأكد من صحة مفتاح API
  - تأكد من الاتصال بالإنترنت
  - تحقق من سجلات الخادم (backend logs) للتفاصيل

### خطأ: "API key not valid"
- **الحل:** 
  - تأكد من نسخ المفتاح بشكل صحيح
  - تأكد من أن المفتاح لم يتم حذفه أو تعطيله
  - قم بإنشاء مفتاح جديد إذا لزم الأمر

### المذكرة المولدة غير دقيقة أو غير كاملة:
- **الحل:** 
  - استخدم زر "🔄 إعادة توليد" لتوليد مذكرة جديدة
  - يمكنك تعديل الحقول يدوياً بعد التوليد
  - تأكد من ملء جميع الحقول بشكل واضح ومفصل

## ملاحظات مهمة

⚠️ **الأمان:**
- لا ترفع ملف `.env` إلى Git أبداً
- مفتاح API يجب أن يبقى سرياً
- قم بتحديث المفتاح بانتظام إذا لزم الأمر

💰 **التكلفة:**
- Gemini API (Gemini Pro) يوفر حد مجاني سخي
- راجع [Google AI Studio Pricing](https://ai.google.dev/pricing) للحصول على تفاصيل الأسعار

🔄 **الحدود:**
- هناك حدود على عدد الطلبات في الدقيقة/الساعة
- إذا تجاوزت الحدود، انتظر قليلاً قبل المحاولة مرة أخرى

## الدعم

إذا واجهت أي مشاكل، تحقق من:
1. سجلات Backend (`nest-backend`)
2. Console في المتصفح (F12)
3. ملف `.env` ومفتاح API






