# حل مشكلة خطأ 504 في تسجيل الدخول

## المشكلة
عند محاولة تسجيل الدخول، يظهر الخطأ:
- `504 (Gateway Timeout)`
- `خطأ في تسجيل الدخول. الرجاء التحقق من البيانات المدخلة.`
- `login.captchaRequired`

## السبب
الخادم الخلفي (Backend Server) غير قيد التشغيل على المنفذ 3000.

## الحل

### الخطوة 1: التأكد من تشغيل قاعدة البيانات

افتح terminal جديد وانتقل إلى مجلد المشروع:

```bash
cd /Users/lebcirakram/Documents/ala-project/nest-backend
```

تحقق من أن قاعدة البيانات تعمل:

```bash
docker compose ps
```

إذا لم تكن قاعدة البيانات تعمل، شغّلها:

```bash
docker compose up -d
```

### الخطوة 2: تشغيل الخادم الخلفي

في نفس Terminal، شغّل الخادم الخلفي:

```bash
npm run start:dev
```

سترى رسائل مثل:
```
[Nest] Starting Nest application...
[Nest] Nest application successfully started on: http://localhost:3000
```

### الخطوة 3: التحقق من عمل الخادم

افتح Terminal آخر واختبر:

```bash
curl http://localhost:3000/api
```

إذا رأيت استجابة (حتى لو كانت خطأ)، فهذا يعني أن الخادم يعمل.

### الخطوة 4: إعادة المحاولة

ارجع إلى المتصفح وجرّب تسجيل الدخول مرة أخرى.

## ملاحظات

- يجب أن يكون الخادم الخلفي قيد التشغيل أثناء استخدام التطبيق
- اترك Terminal الذي يشغّل الخادم الخلفي مفتوحاً
- إذا أغلقت Terminal، سيتوقف الخادم ولن يعمل تسجيل الدخول

## استكشاف الأخطاء

### إذا فشل تشغيل قاعدة البيانات:

```bash
# إيقاف جميع الحاويات
docker compose down

# إعادة التشغيل
docker compose up -d
```

### إذا كان المنفذ 3000 مستخدماً:

```bash
# العثور على العملية التي تستخدم المنفذ
lsof -ti:3000

# إيقافها (استبدل PID برقم العملية)
kill -9 PID
```

### إذا ظهرت أخطاء في Node.js:

تحقق من إصدار Node.js (يجب أن يكون v18 أو أحدث):
```bash
node --version
```

